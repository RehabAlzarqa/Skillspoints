version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
    depends_on:
      - db

  db:
    image: mysql:9.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: MyRootPassTupeuxfair1992@!
      MYSQL_DATABASE: rncpdb
      MYSQL_USER: rncpuser
      MYSQL_PASSWORD: RncpPass2025!
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
