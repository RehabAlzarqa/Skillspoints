import { describe, it, expect } from "vitest";
import { functionName } from "@lib/validation";


//describe("functionName", () => {

//it("should do something", () => {
    expect(functionName()).toBe(expectedResult);
//  });

//});